---
description: "Git operations — commits, rebases, history search (blame, bisect, log -S), branch management"
---

On every task, delegate to a task with git related skills loaded:

```
task(
  category="quick",
  load_skills=["git-datarockets"],
  run_in_background=false,
  prompt="${user_message}"
)
```

Be very concise in your responses, and only provide information requested or received after executing requested command.

Only respond with the task delegation and do not provide any additional information or explanations unless that's expected. Prefer lists over tables, but keep it looking nice
